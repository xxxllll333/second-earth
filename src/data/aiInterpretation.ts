// ═══════════════════════════════════════════════════════════════
// AI 解读存档：开发期由大模型生成的解读文本（与 K2-18b 静态镜像数据版本对应）
//
// 分工原则（避免同一件事被讲多遍）：
//   · 序章 讲"光谱怎么来的"（凌星→指纹→测量，一次故事线）
//   · 辞典 讲"每个术语是什么意思"（就地 ? 锚点，按需浮现）
//   · narrative 讲"当前门槛意味着什么"（滑块下实时反馈）
//   · AI 条 只讲"为什么争议无法裁定"这一深度洞察 + 自由问答
// 真实数字由宿主页 disputeNote 依当前数据算出后拼接，不写死在这里。
// 解读是参考叙述，屏幕上的散点、误差棒与文献仍是第一证据。
// ═══════════════════════════════════════════════════════════════

export const AI_INTERPRET = {
  // 争议为什么存在（概念框）；后半句的数量级由宿主页 disputeNote 用真实数据补上
  dispute:
    '争议的焦点在 3.4 微米处的 DMS 指纹：三个团队面对同一批散点，给出了三条不同的大气解读曲线。这不是谁犯了错，而是前沿科学在此刻的真实状态。',
}
