// ═══════════════════════════════════════════════════════════════
// 🎨 视觉效果调参文件 —— 你不用懂代码，只需要改这里的数字/文字
//
// 使用说明：
// 1. 改完保存（Ctrl + S）
// 2. 浏览器会自动刷新，立刻看到效果
// 3. 颜色用 #开头 + 6位数字字母，例如 #ffffff 是白色、#ff0000 是红色
//    可以去这个网站挑颜色：https://www.schemecolor.com 或搜索"在线取色器"
// 4. 数字改大了/改小了各有说明，照着"← 越大越…"的注释试就行
// ═══════════════════════════════════════════════════════════════

export const VISUALS = {
  // ───────────────────────────────────────────
  // 一、整体底色
  // ───────────────────────────────────────────
  /** 页面背景色。想要"更黑的宇宙"就保持 #0A0A0F，想带点蓝就试 #0A0A18 */
  bgColor: '#0A0A0F',

  // ───────────────────────────────────────────
  // 二、背景星空（那些小亮点）
  // ───────────────────────────────────────────
  /** 背景星点数量。← 数字越大星星越多（1000 以上会开始吃性能） */
  starCount: 800,

  /** 星点最小直径（像素）。← 越大星点越粗 */
  starMinRadius: 0.3,

  /** 星点最大直径（像素）。← 越大越容易出"大颗亮星" */
  starMaxRadius: 1.8,

  /** 星点最低亮度（0~1，0是完全透明，1是不透明） */
  starMinOpacity: 0.2,

  /** 星点最高亮度（0~1） */
  starMaxOpacity: 0.8,

  /** 闪烁最慢速度。← 越大闪得越快 */
  twinkleSpeedMin: 0.005,

  /** 闪烁最快速度。← 越大闪得越快，太大会像在抽风 */
  twinkleSpeedMax: 0.025,

  /** 闪烁幅度（0~1）。改 0 = 完全不闪；改 1 = 亮暗交替最剧烈 */
  twinkleDepth: 0.4,

  // ───────────────────────────────────────────
  // 三、行星光点（彩色大圆点）
  // ───────────────────────────────────────────
  /** 行星整体大小倍数。1 = 现在这样；1.5 = 全部放大一半；0.7 = 缩小 */
  planetSizeScale: 1,

  /** 行星最小直径（像素，在放大倍数之后）。← 越大越小的行星也不会太小 */
  planetMinRadius: 5,

  /** 行星最大直径（像素，在放大倍数之后）。← 越大越大的行星更醒目 */
  planetMaxRadius: 22,

  /** 光晕大小倍数（相对行星自身）。1.4 = 光晕是行星的1.4倍大。← 越大越"雾蒙蒙" */
  glowMultiplier: 1.4,

  /** 鼠标悬停时光晕倍数。← 越大悬停时越亮越炸 */
  hoverGlowMultiplier: 2.0,

  /** 光晕浓度（0~1）。越大光晕越实、越明显；改 0 光晕消失 */
  glowStrength: 1,

  // ───────────────────────────────────────────
  // 四、行星排列
  // ───────────────────────────────────────────
  /** 行星左右间距（像素）。← 越大行星散得越开 */
  planetGapX: 120,

  /** 行星上下间距（像素）。← 越大行与行离得越远 */
  planetGapY: 100,

  /** 行星区域距屏幕边缘的留白（像素）。← 越大整体越往中间挤 */
  padding: 60,

  /** 行星区域整体向下偏移（像素）。← 改小上移、改大下移，避免顶到标题 */
  planetsOffsetY: 40,

  /** 一行最多放几颗。← 改小每行更少更疏，改大更挤 */
  maxCols: 6,

  // ───────────────────────────────────────────
  // 五、标题文字
  // ───────────────────────────────────────────
  /** 顶部大标题文字（直接改引号里的字） */
  titleText: '第二地球 · 系外行星图鉴',

  /** 标题字号（像素） */
  titleSize: '1.3rem',

  /** 标题字距。0.25em = 正常；越大字与字越分开 */
  titleLetterSpacing: '0.25em',

  /** 标题距页面顶部距离（像素） */
  titleTop: 28,

  // ───────────────────────────────────────────
  // 六、底部图例
  // ───────────────────────────────────────────
  /** 图例距页面底部距离（像素） */
  legendBottom: 28,

  /** 图例各项左右间隔（像素） */
  legendGap: 24,

  /** 图例小圆点大小（像素） */
  legendDotSize: 10,

  // ───────────────────────────────────────────
  // 七、悬停信息卡
  // ───────────────────────────────────────────
  /** 信息卡背景色（可以换成半透明深蓝 #0A122080 之类，最后两位是透明度） */
  tooltipBg: 'rgba(10,10,20,0.92)',

  /** 信息卡圆角大小（像素）。← 越大越圆润，改 0 变直角 */
  tooltipRadius: 8,

  /** 信息卡内边距（像素） */
  tooltipPadding: '10px 14px',
}
